# Web verify navigation command menus

Restore the Vite+ Playwright browser-test project and verify that Ctrl+E opens scoped thread/project navigation, project selection reuses drafts, Ctrl+K executes a real command-palette action, and the two global overlays cannot stack.
