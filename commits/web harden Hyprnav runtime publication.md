# Web harden Hyprnav runtime publication

Refresh Corkdiff-backed Hyprnav commands before their websocket tickets expire, publish changed worktree bindings to every known worktree, and preserve a successful durable-save result when best-effort runtime publication throws.

Validate the contract's 255-character binding-name limit in the editor before persistence.
