# Desktop elect registry recovery claims

Replace the shared recovery marker with immutable per-contender claims and elect one live claimant.

This removes the validation/unlink race while retaining crash recovery and registry transaction exclusivity.
