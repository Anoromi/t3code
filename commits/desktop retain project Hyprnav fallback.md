# Desktop retain project Hyprnav fallback

When a projected worktree has disappeared, continue publishing project-scoped Hyprnav bindings at the repository root.

Discard only stale worktree and thread operations, credentials, and locking.
