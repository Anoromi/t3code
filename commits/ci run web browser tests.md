# CI run web browser tests

Install Chromium and execute the web browser-test project on every pull request and main-branch push so navigation, command-palette, and Hyprnav settings regressions cannot bypass the normal test workflow.
