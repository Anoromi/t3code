# Repo complete upstream rebuild provenance ledger

Record every pre-rebuild source and capture commit in its replacement intent note, including source hashes and the focused scenarios retained by that intent. The 29 preexisting fork commits plus five dirty-state capture commits are now fully accounted for; behavior already provided by upstream or intentionally superseded is called out alongside the applicable replacement.
